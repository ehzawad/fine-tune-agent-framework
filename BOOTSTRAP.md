# Repository bootstrap marker

The complete single-A100 xLAM-2 QLoRA implementation is committed on `main`. This marker is intentionally harmless and exists so the repository can also be initialized through the authenticated GitHub connector when the direct Git transport is unavailable.
